/* global controllers*/

'use strict';

controllers.controller('UserManagementController', ['$scope', '$state', 'UserManagementModel', 'ListModel', 'appService',
    function($scope, $state, UserManagementModel, ListModel, appService) {
    console.info('UserManagement executing.');
    

}]);